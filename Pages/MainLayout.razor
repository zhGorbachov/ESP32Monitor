@inherits LayoutComponentBase
@inject DeviceStateHolder StateHolder

@if (StateHolder.IsSimulationMode)
{
    <div class="sim-banner">
        &#9888; Simulation Mode — no real ESP32 is required. Set <code>Esp32:SimulationMode: false</code> in appsettings to connect a real device.
    </div>
}

<div class="app-shell">
    <nav class="sidebar">
        <div class="sidebar-brand">
            <span class="brand-icon">&#128161;</span>
            <span class="brand-text">ESP32 Monitor</span>
        </div>
        <ul class="nav-links">
            <li>
                <NavLink href="/" Match="NavLinkMatch.All">
                    <span class="nav-icon">&#128202;</span> Dashboard
                </NavLink>
            </li>
            <li>
                <NavLink href="/effects">
                    <span class="nav-icon">&#127752;</span> Effects
                </NavLink>
            </li>
            <li>
                <NavLink href="/copyright">
                    <span class="nav-icon">&#169;</span> Авторське право
                </NavLink>
            </li>
        </ul>
    </nav>

    <main class="content-area">
        <div class="topbar">
            <h1 class="page-title">@TitleContent</h1>
        </div>
        <div class="page-body">
            @Body
        </div>
    </main>
</div>

@code {
    [CascadingParameter] private string? TitleContent { get; set; }
}
